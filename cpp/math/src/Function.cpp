#include "math/Function.hpp"
#include <cmath>

namespace mywheels {
  float sqrt(float x) {
    return std::sqrt(x);
  }

  double sqrt(double x) {
    return std::sqrt(x);
  }
} // namespace mywheels